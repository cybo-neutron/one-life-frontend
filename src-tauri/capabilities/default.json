{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:default",
    "sql:default",
    {
      "identifier": "fs:allow-write-text-file",
      "allow": [{ "path": "$HOME/Documents/one-life/**" }]
    },
    {
      "identifier": "fs:default",
      "allow": [{ "path": "$HOME/Documents/one-life/**" }]
    }
  ]
}
